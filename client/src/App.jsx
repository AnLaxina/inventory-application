// TODO: remove all of this and focus on fixing up the React structure

// /client
// ├── /src
// │   ├── /api         <-- Functions to fetch data (fetchItems, deleteItem)
// │   ├── /assets      <-- Images, global CSS
// │   ├── /components  <-- Reusable UI (Button, NavBar, ItemCard)
// │   ├── /pages       <-- Full views (Dashboard, ItemDetails, CreateForm)
// │   ├── /hooks       <-- Custom hooks (optional, e.g., useInventory)
// │   ├── App.jsx      <-- Router setup (React Router DOM)
// │   └── main.jsx     <-- Entry point
// └── vite.config.js

function App() {
    return (
        <>
            <h1>Hello, World!</h1>
        </>
    );
}

export default App;
